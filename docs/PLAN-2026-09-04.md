# Working plan — 2026-09-04

Standing rule for this plan: **nothing new launches on shared infrastructure
(NRP) or spends money without an explicit owner go.** Items are marked
[auto] runs itself, [me] my next actions in order, [owner] needs you,
[gate] blocked until the named thing happens.

## Now in flight (no action needed)

- [auto] CR-I-EIP cell B on Atlas (1.5B x es-bt, patching phase) and cell C
  on NRP (26Gi, properly sized). Monitors watch job status and completion.
- [auto] D8-V1b: five RIPE measurements collecting; evaluation days are
  today and tomorrow (09-04/05). Grading needs the control-plane join re-run
  AFTER the window closes — nothing until 09-06.

## Phase 1 — close the CR-I-EIP arc (next ~24h)

1. [gate: B+C land] Harvest both cells, grade the family as executed
   (verdict already locked FAIL by cell A), write RESULTS-CR-IEIP with the
   rho-regime diagnosis.
2. [me] Draft PREREG-CR-IEIP-V2: the regime-gated conditional claim
   (consumer read beats raw IFF the calibration map passes held-out
   validation; mechanical gate, both arms). FAMILY-CONSTRUCTED = the B/C
   record date; earliest seal the day after.
3. [me] Update the erisml-lib note with the sharpened spec rule (fall back
   to raw / abstain below the rho floor; cell A is the measured consequence).
4. [owner] Fresh-day seal of V2 when eligible — say "seal it" that day.

## Phase 2 — D8-V1b grading (09-06 onward)

5. [gate: window closes 09-06] Re-run the control-plane join on the
   evaluation days, run the sealed checker, commit the verdict as executed.
   VB1 may honestly land below its bar (pre-committed reading exists).

## Phase 3 — FPGA / EPU silicon cell (owner-paced)

6. [owner decision] Tier 1 route: EITHER you open the Coder "U55C Vitis"
   workspace and run ./run_hls_timing.sh (your template's intended path),
   OR you tell me to ship it as a batch job using
   gitlab-registry.nrp-nautilus.io/nrp/coder-images/vivado-vitis.
   I do not submit anything here without that word.
7. [gate: Tier 1 numbers] I draft the silicon-cell predictions (P1 labeled
   replication, P2 consumer-relative onset, P3 the verdict inversion) and
   commit them BEFORE any Tier 2/3 work.
8. [gate: predictions committed + owner go] Tier 2 xclbin build (hours,
   platform string fix from your notes), then Tier 3 on-card measurement
   via your measure_job.yaml pattern.

## Papers (owner-court, deadline order)

- [owner] WCNC submission — deadline 2026-09-15, paper is number-clean.
  The nearest hard date on the board.
- [owner] TMLR CR-ANN paper — submission-ready; needs your OpenReview
  submission + anonymized records bundle (I prepare the bundle on your go).
- [owner] OFC decision (10-20); quarter-turn author email + venue.

## Queued behind the above (no dates, promoted order)

- EC-grid AC false-clear cell (the PES-decisive number) + owner IEEE Xplore
  prior-art pass before any submission.
- OT-finance signature-vol successor cell; then prereg.
- OT-UMAP epsilon-normalized rerun + position-dependent P_C cell; residual
  sweep gaps close before any prereg.
- ConsumerMetric scaffold as a turboquant-pro PR (tool-not-claim rules).
- The embedding-geometry flagship: was gated on CR-ANN V2 passing; V2
  FAILED, so per its own gate it inherits the resolution-limit lesson —
  [owner decision] whether to redesign its bars around measured resolution
  or shelve it.

## Parked (explicitly not being worked)

- Atlas power items awaiting your call: containerd/dockerd deep profile,
  C-states BIOS visit (bundle with next planned reboot), GPU burst caps,
  RAM downsizing. Policies already active: NRP offload for long jobs,
  off-peak scheduling.
- OT-EDA OpenROAD cell, scRNA OT-UMAP, TLE staleness, stream gauges — in
  the DATASETS-MAP, untouched until promoted.

## Discipline notes carried forward

- Measured sizing only (footprints.json guard) for anything on NRP.
- Monitors watch job STATUS, not pods.
- Sweep before build in mined domains; verdicts committed as executed;
  FAILs stay.
